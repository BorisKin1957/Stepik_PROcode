'''Реализуйте функцию find_magic_word(message), которая принимает
одну строку с посланием и:

    разбивает её на слова;

    для каждого слова удаляет знаки препинания только с начала и конца (набор
    знаков берите из модуля String из курса «PROкод: курс по Python для начинающих»);

    определяет, является ли длина очищенного слова простым числом;

    как только встретится первое слово с простой длиной --> выводит это
    слово с помощью print;

    если подходящих слов нет --> выводит None.

Требования:

    функция работает только с переданной строкой (без input() внутри);

    вызывать функцию не нужно, нужно только её определить.

🧪 Пример входные данные

Hello, cosmic traveler!


🧪 Пример вывод

Hello


🧪 Пример входные данные

Greetings, universe!


🧪 Пример вывод

None'''


def find_magic_word(message):
    # Набор знаков препинания
    punctuation = '.,:;?!""«»()[]{}…-—/\\|+*%#@$~`\''
    # Простые числа до 17 (типичная длина слов)
    prime_lengths = {2, 3, 5, 7, 11, 13, 17}

    # Разбиваем сообщение на слова
    for word in message.split():
        # Удаляем знаки препинания с начала
        while word and word[0] in punctuation:
            word = word[1:]
        # Удаляем знаки препинания с конца
        while word and word[-1] in punctuation:
            word = word[:-1]

        # Проверяем длину очищенного слова
        if len(word) in prime_lengths:
            print(word)
            return  # завершаем при первом найденном слове

    # Если не нашли — выводим None
    print(None)