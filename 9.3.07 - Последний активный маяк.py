# Получаем строку с маяками, разбиваем её на элементы и сохраняем в множестве (уникальные значения)
beacons = set(input().split())

# Сохраняем ссылку на метод .pop() множества beacons — это позволяет вызвать его позже
popper = beacons.pop

# Сохраняем ссылку на метод .clear() — он не будет вызван немедленно
cleaner = beacons.clear

# Читаем пустую строку (предположительно, разделитель или заглушка) — игнорируем
input()

# Читаем следующую строку, разбиваем на маяки и удаляем каждый из множества
for beacon in input().split():
    beacons.remove(beacon)  # Удаляет элемент; если его нет — вызовет KeyError

# Считываем число t; если оно не ноль, выполняем блок
if (t := int(input())):
    # Читаем ещё одну строку с маяками и удаляем их, но без ошибок, если их нет
    for beacon in input().split():
        beacons.discard(beacon)  # Удаляет, если есть; если нет — ничего не делает

# Удаляем и возвращаем один произвольный элемент из множества и печатаем его
print(popper())

# очищаем множество
cleaner()

# Выводим текущее состояние пустого множества set()
print(beacons)